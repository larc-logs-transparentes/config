# Configurações para subir em ambiente sala limpa

## Para subir o ambiente

Acessar ``config/hom``:

Executar:

```bash
docker compose up
```

## Para finalizar o ambiente

Acessar ``config/hom``:

Executar:

```bash
docker compose down
```

**Nota**: o Mongo DB será desalocado permanentemente.